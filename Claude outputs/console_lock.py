"""
Общая блокировка на ВЕСЬ консольный вывод программы.

Раньше блокировка жила только в watcher.py и её использовал только StatusBoard/log() — а
notifier.py печатал вопросы пользователю (ask_sync_choice) в обход неё. Пока это не создавало
видимой проблемы только потому, что консольный ввод сам по себе блокирующий, но при печати
самого вопроса (несколько строк подряд) он всё равно мог наложиться на перерисовку столбика.
Вынесено в отдельный модуль, чтобы watcher.py и notifier.py могли использовать одну и ту же
блокировку без циклического импорта друг друга.
"""

import threading

LOCK = threading.Lock()
