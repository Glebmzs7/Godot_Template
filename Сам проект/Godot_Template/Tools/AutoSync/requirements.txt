watchdog>=4.0

# win10toast-reborn — опционально, только для системных всплывающих уведомлений на Windows.
# Не ставим по умолчанию: имя пакета в PyPI может не совпадать/не поддерживаться под вашу
# версию Python. notifier.py и без него работает (падает в обычный print в консоль).
# Если хотите системные уведомления — попробуйте установить отдельно и подобрать рабочий пакет:
#   pip install win10toast
